services:
  api:
    build:
      context: ..
      dockerfile: ./backend/Dockerfile
    ports:
      - "${PORT:-3000}:3000"
    volumes:
      - .:/usr/src/app
    depends_on:
      db:
        condition: service_healthy
    env_file:
      - .env
    environment:
      NODE_ENV: production
      PORT: 3000
      DB_HOST: db
      DB_PORT: 5432
      DB_USER: ${DB_USERNAME:-postgres}
      DB_PASS: ${DB_PASSWORD:-password}
      DB_NAME: ${DB_NAME:-storage_calculator}
    networks:
      - storage-calculator

  db:
    container_name: db
    image: postgres:18.1-bookworm
    environment:
      POSTGRES_PASSWORD: ${DB_PASSWORD:-password}
      POSTGRES_DB: ${DB_NAME:-storage_calculator}
      POSTGRES_USER: ${DB_PASSWORD:-postgres}
    ports:
      - "${DB_PORT:-5432}:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER}"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - storage-calculator

volumes:
  postgres-data:

networks:
  storage-calculator:
    driver: bridge
